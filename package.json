{
  "name": "plot-twist-arena",
  "version": "1.0.0",
  "description": "AI-powered plot twist game with dual modes",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev:frontend\" \"npm run dev:backend\" \"npm run dev:model\"",
    "dev:frontend": "cd frontend && npm run dev",
    "dev:backend": "cd backend && npm run dev",
    "dev:model": "cd model-server && python -m uvicorn main:app --reload --port 8001",
    "build": "npm run build:frontend && npm run build:backend",
    "build:frontend": "cd frontend && npm run build",
    "build:backend": "cd backend && npm run build",
    "install:all": "npm install && cd frontend && npm install && cd ../backend && npm install && cd ../model-server && pip install -r requirements.txt",
    "train:model": "cd model-server && python train_model.py",
    "scrape:dataset": "cd dataset-builder && python scrape_twists.py"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
